SYSTEM:
You are a thesaurus builder assistant. User will give you text that is extracted from pdf documents of the university Machine Learning lectures.
Lectures are given in Russian language, but also can contain english definitions.
After analyzing the text, your should extract the definitions from Machine Learning, Deep Learning and Computer Science that are formulate there.
When making definitions, try to use the text of the lecture as much as possible, don't thinking it out on your own. The input text may be poorly formatted, contain encoding issues, and include formulas, parse it accurately and robustly.
Write the answer using simple markdown syntax, so the output should looks like a markdown list of definitions.

USER:
Attention 
и 
Transformer
Наумов Антон (@any0019)
План лекции
1. NLP – повтор
2. Виды задач
3. Attention
4. Transformer
5. Современный NLP
19.02.2024 лекция 2 (Attention и Transformer) 1
1. NLP - повтор
1. Что такое текст?
2. Эмбеддинги
3. Работа с текстом
4. Особенности NLP
5. RNN
6. LSTM
19.02.2024 лекция 2 (Attention и Transformer) 2
1.1. Что такое текст?
У картинок есть пространственная связь между пикселями.
А у текста есть связь между словами “сквозь время”.
Текст – последовательность слов, имеющая большое количество связей, к пр.:
• Смысловая
• Структура предложения
• Контекст локальный/глобальный
• (Время) Порядок предложений и не только
Но как представить текст для компьютера, чтобы нам было бы удобнее с ним 
работать (в т.ч. с помощью нейросетей)?
19.02.2024 лекция 2 (Attention и Transformer) 3
1.2. Эмбеддинги
Хочется привезти к каким-то математическим структурам, при этом сохранив 
достаточно информации из текста.
1. Word embeddings:
𝑉 = 𝑤𝑖 𝑖 - словарь всех возможных слов
𝑓: 𝑉 → ℝ𝑑 - сопоставляет каждому слову какой-то вектор чисел
2. Text embeddings:
𝑓: 𝑉ℕ → ℝ𝑑 - сопоставляем любому предложению какой -то вектор чисел
В общем хотим для двух текстов (или слов, в случае Word embeddings) 𝑡1 и 𝑡2:
• 𝑓 𝑡1 ≠ 𝑓 𝑡2 , если 𝑡1 ≠ 𝑡2.
• 𝑓 𝑡1 ≈ 𝑓 𝑡2 , если 𝑡1 и 𝑡2 похожи по смыслу.
• 𝑓 𝑡1 ≉ 𝑓 𝑡2 , если 𝑡1 и 𝑡2 не похожи по смыслу.
19.02.2024 лекция 2 (Attention и Transformer) 4
1.2.1. Эмбеддинги (one-hot)
Самый примитивный подход, пусть у нас есть наш словарь из всех слов 𝑉 = 𝑤𝑖 𝑖.
Каждому слову сопоставляем вектор 0, 1 |𝑉|, где 1 ставится только на позиции 
соответствующей индексу данного слова в словаре.
Текст размера 𝐾 слов будет представляться матрицей 0, 1 𝐾×|𝑉|.
Проблемы:
• Очень большой размер представлений.
• Все представления различных слов ортогональны друг -другу, нет понятия 
дистанции.
• Никакой дополнительной информации.
19.02.2024 лекция 2 (Attention и Transformer) 5
1.2.2. Эмбеддинги (Bag Of Words ~ BoW)
Вариант построения эмбеддингов для текстов – сумма one-hot векторов слов.
Плюсы:
• Теперь размер векторного представления текста “всего лишь” ℕ|𝑉|.
• Предложения с похожим набором слов похожи.
Минусы:
• Всё ещё слишком большие представления.
• Похожие слова или похожие предложения, но из разных слов – ортогональны.
• Sparse представления.
• Теряем всю информацию о порядке слов.
19.02.2024 лекция 2 (Attention и Transformer) 6
1.2.3. Эмбеддинги (Word2Vec)
Давайте будем обучать эмбеддинги для слов (или чего -то ещё) с помощью отдельных 
нейросетей.
Идея Word2Vec – будем обучаться предсказывать связь между словом и несколькими 
словами в его ближайшем контексте (фиксированного размера окна).
1. CBoW (Continuous Bag of Words) – предсказываем слово по контексту из окна
2. Skip-gram – предсказываем контекст по слову
19.02.2024 лекция 2 (Attention и Transformer) 7

1.2.3. Эмбеддинги (Word2Vec)
19.02.2024 лекция 2 (Attention и Transformer) 8

1.2.3. Эмбеддинги (Word2Vec)
Плюсы:
• Похожие по контекстам слова оказываются похожи в пространстве эмбеддингов.
• Смысловые зависимости между словами сохраняются при математических 
операциях над эмбеддингами:
f(король) − 𝑓(мужчина) + 𝑓(женщина) ≈ 𝑓(королева)
Минусы:
• Плохо справляется со словами вне словаря (слова не было в словаре – нет никакой 
информации о нём).
• Для однокоренных или составных с общими частями слов эмбеддинги обучаются 
отдельно и никак не используется подобного рода связь между словами.
19.02.2024 лекция 2 (Attention и Transformer) 9
1.2.4. Эмбеддинги (GloVe)
GloVe (Global Vectors) – подход на контекстах, как и Word2Vec, но так же пытается 
учитывать глобальные зависимости.
Пусть 𝑋𝜖ℝ|𝑉|×|𝑉| - матрица частот, где 𝑋𝑖;𝑗 - как часто слово 𝑗 встречалось в 
контексте слова 𝑖. Будем так же обозначать 𝑋𝑖 = σ𝑘 𝑋𝑖;𝑘 и 𝑃𝑖;𝑗 = 𝑃 𝑗 𝑖 =
𝑋𝑖;𝑗
𝑋𝑖
.

ASSISTANT:
* __Текст__ — это последовательность слов, между которыми существует множество связей: **смысловая** (семантика), **структурная** (грамматика и синтаксис), **контекстуальная** (локальный и глобальный контекст), **временная** (порядок предложений и других единиц).

* __Word embedding__ — функция для векторизации слов **f: V → ℝᵈ**,  
которая каждому слову из словаря **V** сопоставляет вектор из **ℝᵈ**.

    Требуется:
    - `f(t₁) ≠ f(t₂)`, если `t₁ ≠ t₂`,
    - `f(t₁) ≈ f(t₂)`, если `t₁` и `t₂` похожи по смыслу,
    - `f(t₁) ≉ f(t₂)`, если `t₁` и `t₂` не похожи по смыслу.

* __Text embedding__ — функция  **f: Vⁿ → ℝᵈ**, которая сопоставляет целому предложению/тексту вектор признаков.

* __One-hot embedding__ — представление слова как вектора длины `|V|` с одной единицей и остальными нулями.

    Проблемы:
    - большой размер вектора,
    - все слова ортогональны,
    - нет расстояний между словами,
    - нет обобщающей информации.

* __BoW__ (__Bag of words__) —  представление текста как суммы one-hot векторов всех слов.
    Плюсы:
    - компактнее one-hot,
    - предложения с похожим набором слов близки.

    Минусы:
    - высокая размерность,
    - теряется порядок слов,
    - игнорируются семантические связи.

* __Word2Vec__ — обучаемая модель эмбеддингов слов на основе контекста.

    Два основных варианта:
    - **CBoW (Continuous Bag of Words)** — предсказывает слово по контексту.
    - **Skip-gram** — предсказывает контекст по слову.

    Плюсы:
    - контекстно-похожие слова имеют схожие вектора
    - сохраняются смысловые отношения, например:  
    `f(король) - f(мужчина) + f(женщина) ≈ f(королева)`

    Минусы:
    - не работает со словами вне словаря,
    - не учитывает морфологические или составные связи.

* __GloVe__ — модель, объединяющая локальный контекст и глобальные частоты.

    Построение:
    - **X ∈ ℝ^{|V| × |V|}** — матрица совместных частот:  
    `Xᵢⱼ` — сколько раз слово `j` встречалось в контексте слова `i`.
    - **Xᵢ = ∑ₖ Xᵢₖ** — сколько всего слов встречается в контекстах слова `i`.
    - **Pⱼ|ᵢ = Xᵢⱼ / Xᵢ** — условная вероятность слова `j` при условии `i`.
